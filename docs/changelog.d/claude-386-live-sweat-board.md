- **#386** Live Sunday sweat board — during games, `/picks` now shows in-game scores with each locked pick's live cover state, a live "week so far" projection, and a freshness stamp that flips to a reconnecting state when data goes stale. Display-only: it's a self-gated pass-through over the ESPN scoreboard with short-interval client polling and a shared cache — no new cron, no DB change, and grading stays the sole settlement authority.
