- **PR #603** ADR-0033 (client-query data loading) marked Accepted — ratifies the client-side query/cache data-loading pattern (cached keyed reads that render instantly and revalidate in the background) as the standard for read surfaces.
