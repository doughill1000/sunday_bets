- **PR #840** Drop the week-card score from `text-2xl` to `text-base` so it matches the
  matchup title's weight instead of towering over sixteen cards a week; the score stays
  the card's anchor by position, not by size. files: `WeeklyPickCard.svelte`
