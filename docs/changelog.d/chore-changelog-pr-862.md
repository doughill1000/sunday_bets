- **PR #862** Added the missing changelog entry for PR #859, unblocking the governance
  freshness sweep. Keyed by PR number because it closed no issue — which is also why it
  went on to trip the same sweep itself; this entry closes that loop.
  files: `docs/changelog.d/`
