- **#739** Off-season `/recap` archive is reachable — the Season recaps page now anchors to the last season with results (via `resolveSeasonYear`) instead of the empty calendar season, honors `?season=`, and gains a minimal season select so past archives are reachable. Recap push notifications and the `/league` Week tab / honors links now deep-link with a season-qualified `?season=YYYY#week-N` so they stay unambiguous once a new season starts grading. routes: `/recap` · files: `WeeklyHardware` link producers · `notifications.ts`
