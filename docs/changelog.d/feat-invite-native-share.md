- **PR #613** Native share for invite links — the commissioner Invites panel now offers a
  "Share" action that opens the mobile OS share sheet pre-filled with the invite URL, with
  clipboard copy (now with a "Copied" confirmation) as the fallback on desktop or when the
  sheet is dismissed. route: `/league/manage`
