- **#727** Hoist the reigning champion into an evergreen banner above the `/league` tabs —
  it used to render only at the bottom of the Standings tab and was absent from Week
  entirely. `LeagueHonors` now opens on the trophy case instead. files:
  `src/lib/components/group/ReigningChampionBanner.svelte` ·
  `src/routes/(app)/league/+page.svelte` · `src/lib/components/group/LeagueHonors.svelte`
