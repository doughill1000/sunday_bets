- **#742** One season-story promoter — Season Wrapped and the weekly recap no longer
  both interrupt the same app open; a single dispatcher lets at most one through, with
  Wrapped ranked first and the loser resurfacing on a later open. Each flash keeps its
  own seen marker. files: `src/lib/ui/seasonStoryFlash.ts` · `src/routes/+layout.svelte`
