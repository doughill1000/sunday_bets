- **PR #614** PWA mobile polish — a cleaner installed-app launch: iOS standalone metas
  (`apple-mobile-web-app-*` + `mobile-web-app-capable`) give a full-screen launch with a
  dark status bar and the "Hotshot" title, and the web manifest gains a stable `id` plus
  app shortcuts ("Make picks", "Standings") for the Android long-press menu. files:
  `src/app.html` · `vite.config.ts`
