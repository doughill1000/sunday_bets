- **#585** Demo live sweat board — the public `/demo` season now bakes a frozen mid-game
  week, so the live picks sweat (per-pick cover verdicts, week-so-far, group dots, and a
  Final — unofficial game) and the Weekly-tab provisional standings render on demand,
  without waiting for a real game window. routes: `/demo`, `/demo/leaderboard` · fixture:
  `demo-snapshot.json` · ADR-0026
