- **#737** /league Standings: honors and the credibility ladder are now always
  co-visible — a bare visit defaults to the season window year-round (offseason pins
  "Last season · YYYY", never a silent All-time flip), a concluded season leads with its
  honors, the career-grain ladder renders beneath the table in both scopes, and All-time
  became a shareable `?scope=alltime` URL. The WrappedPromo banner retired into the
  honors card's Wrapped link. Mirrored on /demo/league; season-pin util shared with
  /stats; ADR-0032 §9 semantics untouched (surfacing amendment note); both ordering
  rules recorded in docs/DESIGN.md.
