- **PR #580** Backfill PR #571's missing changelog entry — #571 (the changelog squash)
  merged after the enforcement cutoff it introduced, so the governance gate flagged it as
  drift on every later PR; added its entry. docs-only. file: `docs/CHANGELOG.md`
