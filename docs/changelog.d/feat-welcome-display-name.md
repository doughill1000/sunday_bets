- **PR #615** Confirm your name in the welcome guide — the first-run guide now carries a
  prefilled "Your name" field so email signups (who inherit their email local-part as the
  leaderboard display name) can pick a friendly one once; it saves via the profile API
  only when changed. component: `WelcomeGuide`
