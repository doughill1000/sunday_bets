- **#710** Migration generator improvements — added a supported delete/rename path (`--retire=<key>` synthesizes the drop and removes the ledger entry), a duplicate-numeric-prefix guard (existing collisions warn, new ones error), a `-- @phase:` header to override a source file's emission phase, `--amend` to redo the newest uncommitted migration, and required migration names plus signature-extraction warnings. Generator tooling, tests, and docs only — no schema change. (`supabase/scripts/generate-migration.ts`, ADR-0011/0012)
