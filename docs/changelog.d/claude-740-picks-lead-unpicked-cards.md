- **#740** `/picks` now leads with the unpicked game cards — the summary bar is followed
  directly by the cards grid, with the status board and All-In declarations moved below.
  Pure reorder of `PicksBoard.svelte`; nothing added or removed.
