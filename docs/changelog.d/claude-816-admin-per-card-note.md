- **#816** Each `/admin` card now renders its own result inline, next to its own
  controls, instead of one shared status note stuck below all six cards (off-screen on
  mobile). Triggering an action on one card no longer clears or overwrites another
  card's still-relevant result.
