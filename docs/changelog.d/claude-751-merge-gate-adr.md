- **#751** Recorded the PR merge-gate model as ADR-0038 — the `-result` wrapper-job
  contract that makes a path-filtered CI job usable as a required status check, and the
  rule that required-check names are a stable interface with the repo's GitHub ruleset.
