- **#716** Add `prod-backfill` skill — a written runbook (announce → go-ahead →
  baseline → execute → verify → record) for one-off production data writes, so the
  discipline no longer lives only in agent conversation memory. `season-ops` and
  `db-deep-scan` now cross-link to it.
