- **#638** Season dropdowns default to career/all-time in the off-season — `/league`, `/stats`, and `/market` no longer preselect a season that has already finished, and the newest season only keeps its "This season" pin while it is actually in progress. In-progress is now a real signal read from the season's scoring weeks rather than inferred from "the most recent season with data". An explicit `?season=` still wins. Touches `src/lib/server/db/queries/seasonProgress.ts` and the three season-scoped routes.
