- **#636** Add Backdoor of the Week — the mirror of Bad Beat, crowning the win that barely
  covered instead of the loss that almost did. Derived on read from the same weekly-hardware
  matview, so it shows up wherever Bad Beat already does. files: `src/lib/domain/weeklyAwards.ts`
  · `src/lib/components/recap/WeeklyHardware.svelte`
