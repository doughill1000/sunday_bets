- **#692** Reshape `/market` to its lean form — the page is now three cards in pick order:
  the week slate (with a new divisional tag), the "Where the market bends" synthesis with a
  data-derived verdict line, and the team ATS book with streak chips and a splits-only
  drill-down. The deep explorer is retired: the season/pooled scope dial, the
  Favorites/Spread/Primetime/Divisional slices, and the per-team game logs (component,
  endpoint, and query all deleted). `/demo/market` mirrors the lean shape. Product
  rationale: the 2026-07-16 audit's Reshape verdict, confirmed by the corrected
  post-#734/#735 numbers (every situational cut within noise of a coin flip). Read models
  unchanged (ADR-0017).
