- **PR #620** Rename the apex credibility-rating tier Shark → Hotshot — the top band now reads as the app's own name (a group's Hotshot is its sharpest bettor) instead of the poker-borrowed "Shark", and earns a distinct pill on the `/stats` Career band one step louder than Sharp. Cutoffs unchanged; governed by ADR-0032 (amendment §5). files: `src/lib/domain/rating.ts` · `src/lib/components/stats/RatingBand.svelte`
