- **#361** Career "who knows ball" credibility rating — a persisted, cross-season ELO-style number that survives the season reset. A pure, deterministic fold over settled spread picks (conviction-weighted, soft season reset, hidden until qualified) rebuilds a `player_ratings` read model after each grade; it leads the Career hero on `/stats` with a tier word, season-delta arrow, and a first-class Unrated state. Governed by ADR-0032.
