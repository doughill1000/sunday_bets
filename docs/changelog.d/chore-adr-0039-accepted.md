- **PR #807** Flip ADR-0039 to Accepted — the ADR shipped with #782 but kept its
  `Proposed` status after that issue closed, which failed the governance freshness check
  on every subsequent PR rather than only its own. files: `docs/adr/`
