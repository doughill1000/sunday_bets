- **PR #728** `issue-author` now writes an `Execution (model / effort)` recommendation
  into every issue it creates (mirroring the ad hoc convention on #692/#727), and
  `start-issue` reads it instead of guessing cold; `docs/WORKFLOW.md`'s Ready-issue
  checklist requires it too.
