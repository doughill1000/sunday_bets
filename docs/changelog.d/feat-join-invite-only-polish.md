- **PR #612** Invite-only onboarding screen — the groupless `/join` page is no longer a
  dead-end: it takes a pasted invite link or bare code and routes to the existing
  `/join/[code]` redemption flow, offers a "Try the demo" escape hatch, and keeps the
  create-group card for granted accounts. route: `/join`
