-- generated by supabase/scripts/generate-migration.ts
set check_function_bodies = off;
set client_min_messages = warning;


-- file: schemas/0300_rls.sql
-- Superseded by supabase/src/policies/* (RLS enable: 10_rls_enable.sql; policies:
-- 20-51_policies_*.sql). This file used to declare the entire RLS surface directly;
-- it was split into per-concern files under policies/ so tenancy/security additions
-- didn't require touching one growing bundle. Because the generator emits
-- schemas/ before policies/ (SOURCE_ORDER in generate-migration.ts), the policies/
-- versions always ran later and won at apply time, making this file a dead second
-- copy that had already drifted (missing del_picks_own_pre and every policy added
-- after the split). Left as an empty, source-controlled placeholder rather than
-- deleted: the hash-ledger generator treats a removed source file as an error
-- ("deleted or moved SQL sources detected") unless its ledger entry is explicitly
-- retired, and a placeholder avoids reusing "0300_rls.sql" for an unrelated object
-- later. Do not add policy/RLS-enable statements here — add them under
-- supabase/src/policies/ instead.
